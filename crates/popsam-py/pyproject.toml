[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "popsam"
version = "0.1.0"
description = "Representative text sampling from embeddings"
requires-python = ">=3.9"

[tool.maturin]
module-name = "popsam"
